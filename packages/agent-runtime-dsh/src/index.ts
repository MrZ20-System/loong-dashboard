/**
 * Stage 0 reserves this package as the only DSH integration boundary.
 * Runtime lifecycle and event mapping are intentionally deferred to Stage 4.
 */
export const DSH_RELEASE = "dsh-v0.1.2-alpha.5" as const;
